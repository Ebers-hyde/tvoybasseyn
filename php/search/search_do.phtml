<?php
/**
 * Страница с результатами поиска.
 *
 * @var umiTemplaterPHP|ViewPhpExtension|DemomarketPhpExtension $this
 * @var array $variables
 */

$data = $this->getSearchResult();
$dataProducts = $this->getSearchResultProducts();
$contentModule = 'index';
if ($variables['module'] == 'catalog' || $variables['domain-id'] == 2) $contentModule = 'catalog';
?>


<div class="center-wrap pt-2">
<div class="pool-filters__title">Найдено <?=$contentModule == 'catalog' ? umiCount($dataProducts['lines']) . ' товаров' : umiCount($data['lines']) . ' страниц'?></div>
</div>

<main class="center-wrap pt-2">

        <section class="pool-filters">


				<?php if (umiCount($data['lines']) > 0 || umiCount($dataProducts['lines']) > 0): ?>
					<?= $this->render([$data,$dataProducts,$contentModule], "search/result") ?>
				<?php endif; ?>
    

		</section>

</main>